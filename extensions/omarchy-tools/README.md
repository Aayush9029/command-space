# Omarchy Tools

Forms and commands for installed Omarchy scripts, including app installation, media, sharing, desktop settings, and system setup. Privileged operations open a terminal for authentication. Cancel stops work that has not moved to that terminal.

Run inside Omarchy:

```sh
bun test --timeout 60000 extensions/omarchy-tools/tests
```

Tests use disposable homes and recorded system commands.
